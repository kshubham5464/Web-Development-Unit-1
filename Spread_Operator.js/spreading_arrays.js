// �� Use Case: Helps in copying or merging arrays.
// �� Example: arrayCopy(array1, array2);
// �� Returns: A new array that is a copy of the original array.

const numbers = [1, 2, 3];

const newNumbers = [...numbers, 4, 5];
console.log(newNumbers); // [1, 2, 3, 4, 5]
